// Tailwind v4 does NOT use this file — configuration lives in globals.css @theme block.
// This file is kept only to prevent "missing tailwind.config" errors from old tooling.
import type { Config } from "tailwindcss";
const config: Config = { content: [] };
export default config;
